{
  "name": "chat-ext-framework",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "zip": "node scripts/zip.mjs",
    "build:zip": "npm run build && npm run zip",
    "clean": "rm -rf dist build.zip"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
